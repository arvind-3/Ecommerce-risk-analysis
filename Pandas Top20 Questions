
#1. 
Find the number of rows and columns.
df.shape

#2. 
Check data types and non-null values.
df.info()

#3. 
Find missing values in each column.
df.isnull().sum()

#4.
Identify duplicate rows.
df.duplicated().sum()

#5.
Calculate total revenue.
df["order_value_eur"].sum()

#6. 
Find average order value.
df["order_value_eur"].mean()

#7.
Display top 10 orders by value.
df.nlargest(10, "order_value_eur")

#8.
Calculate revenue generated by each country.
df.groupby("country")["order_value_eur"].sum().sort_values(ascending=False)

#9.
Count orders in each product category.
df["product_category"].value_counts()
#10
Find revenue generated by each category.
df.groupby("product_category")["order_value_eur"].sum()

#11
Find the most frequently used payment method.
df["payment_method"].value_counts()

#12.
Find all fraud orders.
fraud_orders = df[df["is_fraud"] == 1]
fraud_orders.head()

#13. 
Calculate fraud percentage.
(df["is_fraud"].sum() / len(df)) * 100

#14. Fraud Cases by Country
Question

Identify countries with highest fraud cases.

df[df["is_fraud"] == 1]["country"].value_counts()
#15.
Find all returned orders.
returned_orders = df[df["is_returned"] == 1]
returned_orders.head()

#16. 
Calculate return percentage.
(df["is_returned"].sum() / len(df)) * 100

#17.
Find average discount offered by each category.
df.groupby("product_category")["discount_percent"].mean()

#18. 
Find customers who spent the most.
df.groupby("customer_id")["order_value_eur"]\
.sum()\
.sort_values(ascending=False)\
.head(5)

#19.
Check correlation among numerical features.
df.corr(numeric_only=True)

#20Revenue Trend by Month
df["order_date"] = pd.to_datetime(df["order_date"])
df.groupby(df["order_date"].dt.month)["order_value_eur"]\
.sum()
